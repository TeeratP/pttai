# Everything the demo needs so the one-command run works:
#   pip install -r demo/requirements.txt && python demo/app.py
# Run locally from a clone (pttai resolves from source). On a Hugging Face
# Space, uncomment the pttai line below so pttai installs from git — nothing
# else from the repo is needed (app.py vendors an offline get_llm fallback).
gradio>=5,<6
langgraph>=1,<2
langchain-core>=1,<2
pydantic>=2,<3

# --- Hugging Face Space only: install pttai from git (uncomment on the Space) ---
# pttai @ git+https://github.com/TeeratP/agentic-framework.git
